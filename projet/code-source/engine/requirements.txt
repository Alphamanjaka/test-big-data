# Moteur d'identité + gouvernance (déduplication et consentement)
# Compatible Python 3.8 (VM Big Data) — RapidFuzz est requis.
pandas>=2.0,<3.0
psycopg[binary]>=3.2,<4.0
python-dotenv>=1.0,<2.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
rapidfuzz>=3.0,<5.0
pydantic>=2.0,<3.0

# Optionnel (montée en charge Spark) : pyspark==3.4.2 est fourni par la VM.